name: CI/CD Pipeline

on:
  push:
    branches: [main]
    paths:
      - 'services/**'
  workflow_dispatch:

env:
  AWS_REGION: us-east-1
  ECS_CLUSTER: medisupply-cluster

jobs:
  # Detect which services changed
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      orders-changed: ${{ steps.changes.outputs.orders }}
      inventory-changed: ${{ steps.changes.outputs.inventory }}
      prometheus-changed: ${{ steps.changes.outputs.prometheus }}
      grafana-changed: ${{ steps.changes.outputs.grafana }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - uses: dorny/paths-filter@v2
        id: changes
        with:
          filters: |
            orders:
              - 'services/orders-service/**'
            inventory:
              - 'services/inventory-service/**'
            prometheus:
              - 'services/prometheus/**'
            grafana:
              - 'services/grafana/**'

  # Build and deploy Orders Service
  deploy-orders:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.orders-changed == 'true'
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::501257812093:role/medisupply-github-actions-role
          role-session-name: GitHubActions-OrdersService
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: medisupply/orders-service
          IMAGE_TAG: ${{ github.sha }}
        run: |
          cd services/orders-service

          # Build image
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest

          # Push images
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

          echo "✅ Orders service image pushed to ECR"
          echo "📦 Image URI: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

      - name: Deploy to ECS via CodeDeploy Blue-Green
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: medisupply/orders-service
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🚀 Starting Blue-Green deployment for Orders Service..."

          # Use the deployment script for proper blue-green deployment
          chmod +x scripts/deploy-service.sh
          ./scripts/deploy-service.sh orders-service $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "✅ Orders Service Blue-Green deployment initiated"
          echo "🔄 Linear traffic shifting: 10% every minute over 10 minutes"

  # Build and deploy Inventory Service
  deploy-inventory:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.inventory-changed == 'true'
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::501257812093:role/medisupply-github-actions-role
          role-session-name: GitHubActions-InventoryService
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push Docker image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: medisupply/inventory-service
          IMAGE_TAG: ${{ github.sha }}
        run: |
          cd services/inventory-service

          # Build image
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest

          # Push images
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

          echo "✅ Inventory service image pushed to ECR"
          echo "📦 Image URI: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG"

      - name: Deploy to ECS via CodeDeploy Blue-Green
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: medisupply/inventory-service
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🚀 Starting Blue-Green deployment for Inventory Service..."

          # Use the deployment script for proper blue-green deployment
          chmod +x scripts/deploy-service.sh
          ./scripts/deploy-service.sh inventory-service $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "✅ Inventory Service Blue-Green deployment initiated"
          echo "🔄 Linear traffic shifting: 10% every minute over 10 minutes"

  # Deploy Prometheus (using official image)
  deploy-prometheus:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.prometheus-changed == 'true'
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::501257812093:role/medisupply-github-actions-role
          role-session-name: GitHubActions-Prometheus
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy Prometheus with official image
        run: |
          echo "🚀 Deploying Prometheus using official Docker image with rolling update..."

          # Update ECS service with new task definition using official image
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service medisupply-prometheus \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Prometheus rolling update deployment initiated"
          echo "📦 Using official image: prom/prometheus:latest"

  # Deploy Grafana (using official image)
  deploy-grafana:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: needs.detect-changes.outputs.grafana-changed == 'true'
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::501257812093:role/medisupply-github-actions-role
          role-session-name: GitHubActions-Grafana
          aws-region: ${{ env.AWS_REGION }}

      - name: Deploy Grafana with official image
        run: |
          echo "🚀 Deploying Grafana using official Docker image with rolling update..."

          # Update ECS service with new task definition using official image
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service medisupply-grafana \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Grafana rolling update deployment initiated"
          echo "📦 Using official image: grafana/grafana:latest"

  # Deploy all services (manual trigger)
  deploy-all:
    runs-on: ubuntu-latest
    if: github.event_name == 'workflow_dispatch'
    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::501257812093:role/medisupply-github-actions-role
          role-session-name: GitHubActions-DeployAll
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build and push all services
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🚀 Building and pushing all services..."

          # Build Orders Service
          cd services/orders-service
          docker build -t $ECR_REGISTRY/medisupply/orders-service:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/medisupply/orders-service:$IMAGE_TAG $ECR_REGISTRY/medisupply/orders-service:latest
          docker push $ECR_REGISTRY/medisupply/orders-service:$IMAGE_TAG
          docker push $ECR_REGISTRY/medisupply/orders-service:latest
          echo "✅ Orders service pushed"

          # Build Inventory Service
          cd ../inventory-service
          docker build -t $ECR_REGISTRY/medisupply/inventory-service:$IMAGE_TAG .
          docker tag $ECR_REGISTRY/medisupply/inventory-service:$IMAGE_TAG $ECR_REGISTRY/medisupply/inventory-service:latest
          docker push $ECR_REGISTRY/medisupply/inventory-service:$IMAGE_TAG
          docker push $ECR_REGISTRY/medisupply/inventory-service:latest
          echo "✅ Inventory service pushed"

          echo "✅ Only microservices (orders & inventory) are built - monitoring uses official images"

      - name: Deploy all services via CodeDeploy Blue-Green
        run: |
          echo "🚀 Starting Blue-Green deployment for all services..."

          # Make deployment script executable
          chmod +x scripts/deploy-service.sh

          # Deploy all services with blue-green deployment
          echo "📦 Deploying Orders Service..."
          ./scripts/deploy-service.sh orders-service $ECR_REGISTRY/medisupply/orders-service:$IMAGE_TAG

          echo "📦 Deploying Inventory Service..."
          ./scripts/deploy-service.sh inventory-service $ECR_REGISTRY/medisupply/inventory-service:$IMAGE_TAG

          echo "📦 Deploying Prometheus with rolling update..."
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service medisupply-prometheus \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "📦 Deploying Grafana with rolling update..."
          aws ecs update-service \
            --cluster ${{ env.ECS_CLUSTER }} \
            --service medisupply-grafana \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ All services deployments initiated!"
          echo "🔄 Microservices using Blue-Green deployment (10% every minute over 10 minutes)"
          echo "📊 Monitoring services using rolling update deployment"